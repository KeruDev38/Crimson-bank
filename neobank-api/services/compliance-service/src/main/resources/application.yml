spring:
  application:
    name: compliance-service
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:compliance}
    username: ${DB_USERNAME:compliance_user}
    password: ${DB_PASSWORD:compliance_password}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: true
  flyway:
    enabled: true
    baseline-on-migrate: true

server:
  port: ${SERVER_PORT:8084}

management:
  endpoints:
    web:
      exposure:
        include: health,info

jwt:
  secret: ${JWT_SECRET:identityLocalDevSecretKeyThatIsAtLeast32Chars}

internal:
  service-token: ${INTERNAL_SERVICE_TOKEN:localInternalServiceTokenThatIsAtLeast32Chars}
